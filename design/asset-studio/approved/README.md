# Approved Design Masters

Visually accepted masters and their generation metadata live here. Approval in
this directory means `approved-design`, not automatically `runtime-ready`.
Runtime promotion still requires the repository asset manifest, provenance,
deterministic derivatives, and validation described in
`docs/assets/asset-migration-spec.md`.
