# Prompts

Store one Markdown or JSON record per generation batch. Preserve the complete
prompt, negative prompt, model/version, seed when available, input references,
dimensions, and post-processing steps.
