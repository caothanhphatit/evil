# References

Create one directory per asset family. Keep a small `README.md` beside each
reference set that records source, version, capture context, and what the image
actually proves. A visual resemblance is not a mapping claim.

Do not copy files out of `game-assets/source` or `game-assets/extracted` merely
for convenience; link to their repository paths from the family brief.
